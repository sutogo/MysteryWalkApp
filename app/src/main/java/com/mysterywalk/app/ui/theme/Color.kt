package com.mysterywalk.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────
//  "Neon Explorer" カラーパレット
//  コンセプト: 深夜の都市探索 × サイバーパンク
// ─────────────────────────────────────────────

// --- 背景レイヤー ---
val DeepSlate        = Color(0xFF0F172A)   // 最も深いダーク背景
val MidnightBlue     = Color(0xFF1E293B)   // カード・サーフェス用
val SurfaceVariant   = Color(0xFF263448)   // 少し明るいサーフェス

// --- ネオンシアン（メインアクセント）---
val NeonCyan         = Color(0xFF00F0FF)   // 主アクセント：鮮やかなシアン
val NeonCyanDim      = Color(0xFF00B8C8)   // やや落ち着いたシアン
val NeonCyanGlow     = Color(0x4000F0FF)   // グロー用（半透明）

// --- パープル（サブアクセント / 将来の切り替え用）---
val ElectricPurple   = Color(0xFFA855F7)
val ElectricPurpleDim = Color(0xFF7C3AED)

// --- ゴールド（XP・レベル表示）---
val GoldBright       = Color(0xFFFFD700)
val GoldDim          = Color(0xFFE6A300)
val GoldGlow         = Color(0x40FFD700)   // グロー用（半透明）

// --- テキスト ---
val OnDark           = Color(0xFFE2E8F0)   // メインテキスト（白に近いスレート）
val OnDarkSecondary  = Color(0xFF94A3B8)   // サブテキスト（ミディアムスレート）

// --- エラー ---
val NeonRed          = Color(0xFFFF4466)

// --- 星屑・パーティクル ---
val StarWhite        = Color(0xCCFFFFFF)
val StarDim          = Color(0x66FFFFFF)
